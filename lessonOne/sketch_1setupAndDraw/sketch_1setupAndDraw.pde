// The two methods void setup() {} and void draw() {} are crucial to every sketch you do.
void setup() {
  // Setup is called once, at the start of your program
  // Setting the size of a window is how you create a window.
  // 1. Create a window that is 400 pixels by 400 pixels. Here's a hint: size(width, height);
  // 2. Set the background to any color you would like. Here's a hint: background(red, green blue); - it uses RGB
  // 3. Press the play button to see it work!
}

void draw() {
  // Draw is called continuously
  // You would use it for things that change, like variables and drawing moving shapes
}
